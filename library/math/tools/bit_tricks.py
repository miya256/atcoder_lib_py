x = 0

_ = x & -x  # 右端の1だけ残す
_ = x & (x - 1)  # 右端の1を消す
_ = ~x  # bit反転（-x-1 とおなじ？）


def f(s):  # sの部分集合全列挙
    # 0含めない
    t = (s - 1) & s
    while t > 0:
        t -= 1
        t &= s
